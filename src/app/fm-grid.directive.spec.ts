import { FmGridDirective } from './fm-grid.directive';

describe('FmGridDirective', () => {
  it('should create an instance', () => {
    const directive = new FmGridDirective();
    expect(directive).toBeTruthy();
  });
});
